@using Models.StaticData
@model IEnumerable<Models.Employee>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<div class="row">
    <div class="col-7">

        <form asp-action="index">
            <div class="form-group col-5">
                <label for="FoundationId" class="control-label">رقم المؤسسة</label>
                <input name="FoundationId" class="form-control" value="@ViewBag.Search.FoundationId" />
            </div>
            <div class="form-group col-5">
                <label for="Name" class="control-label">اسم الموظف</label>
                <input name="Name" class="form-control" value="@ViewBag.Search.Name" />
            </div>
            <div class="form-group col-5">
                <label for="Job" class="control-label">الوظيفة </label>
                <input name="Job" value="@ViewBag.Search.Job" class="form-control" />
            </div>
            <div class="form-group col-5">
                <label for="Department" class="control-label">الادارة العامة</label>
                <input name="Department" value="@ViewBag.Search.Department" class="form-control" />
            </div>
            <div class="form-group col-5">
                <label for="SectorId" class="control-label">القطاع</label>
                <select class="form-select" name="SectorId">
                    @{
                        if (ViewBag.Search.SectorId == null)
                        {
                            <option value="" selected>غير محدد</option>
                        }
                        else
                        {
                            <option value="">غير محدد</option>
                        }
                        foreach (var item in ViewBag.Sector)
                        {
                            if (ViewBag.Search.SectorId == item.Id)
                            {
                                <option value="@item.Id" selected>@item.Name</option>
                            }
                            else
                            {
                                <option value="@item.Id">@item.Name</option>
                            }

                        }
                    }
                </select>
            </div>
            <div class="form-group col-5">
                <label for="DegreeId" class="control-label">الدرجة</label>
                <select class="form-select" name="DegreeId">
                    @{
                        if (ViewBag.Search.DegreeId == null)
                        {
                            <option value="" selected>غير محدد</option>
                        }
                        else
                        {
                            <option value="">غير محدد</option>
                        }
                        foreach (var item in ViewBag.Degree)
                        {
                            if (ViewBag.Search.DegreeId == item.Id)
                            {
                                <option value="@item.Id" selected>@item.Name</option>
                            }
                            else
                            {
                                <option value="@item.Id">@item.Name</option>
                            }

                        }
                    }
                </select>
            </div>
            <div class="form-group col-5">
                <label for="WorkPlace" class="control-label">مكان العمل</label>
                <input name="WorkPlace" class="form-control" value="@ViewBag.Search.WorkPlace" />
            </div>

            <input type="submit" value="search" class="btn btn-primary" />
        </form>
        <div class="col-2">
            <a type="button" asp-action="index" class="btn btn-danger "> reset</a>
        </div>
    </div>
</div>
<table class="table table-striped table-bordered text-nowrap">
    <thead class="bg-primary text-white" style="background-color:#5D7B9D  ">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FoundationId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Gender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Major)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Job)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Department)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WorkPlace)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhoneNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Belong)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CompanyNameForForeign)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sector)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Degree)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FoundationId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                    @{
                        if(item.Gender.HasValue)
                        {

                            @StaticData.gender[item.Gender.Value]
                        }

                    }
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Major)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Job)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Department)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkPlace)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                    @{
                        if (item.Belong.HasValue)
                        {
                            @StaticData.belong[item.Belong.Value]
                            ;
                        }
                    }
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyNameForForeign)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sector!.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Degree!.Name)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" type="button" class="btn btn-dark">Edit</a> 
                    <a asp-action="Details" asp-route-id="@item.Id" type="button" class="btn btn-info ">Details</a> 
                    <button type="button" class="btn btn-danger " data-bs-toggle="modal" data-bs-target="#deleteModal" data-employeeid="@item.Id">
                        Delete
                    </button>
                            <a asp-action="Courses" asp-route-id="@item.Id" type="button" class="btn btn-success ">الدورات</a>
            </td>
        </tr>
}
    </tbody>
</table>

<!-- Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">Confirm Delete</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to delete this employee ?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <form id="deleteForm" method="post" action="/manage/employee/delete/">
                    <input type="hidden" name="id" id="employeeIdInput" value="" />
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    var deleteModal = document.getElementById('deleteModal');
    deleteModal.addEventListener('show.bs.modal', function (event) {
        var button = event.relatedTarget;
        var id = button.getAttribute('data-employeeid');
        var input = document.getElementById('employeeIdInput');
        input.value = id;
    });
</script>
